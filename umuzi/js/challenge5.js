var arr = '';

function removeDuplicates(str1) {
    //a function that removes any dupliacted letters from a word 
}

function compareStrings(str1, str2) {
    var i = 0;
    while (i < str1.length - 1) {
        if (str2.search(str[i])) {
            arr = arr.push(str[i]);
        }
        else {
            i++;
        }
        i++;
    }
    //The remove duplicates step should follow here
    
    return arr;
}
compareStrings('These are the strings', 'To be compared to each other');